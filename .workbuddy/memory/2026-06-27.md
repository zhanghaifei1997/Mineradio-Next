# 2026-06-27

## 图标库生成
- 分析了 Mineradio-Next 项目整体功能（Electron+Vue3 沉浸式音乐播放器）
- 发现项目大量使用 Emoji 作为图标，视觉不一致
- 创建了 `src/components/icons/` 目录，包含 36 个 Lucide 风格 Vue SFC 图标组件
- 图标覆盖 7 个分类：播放控制(6)、播放模式(4)、音量(2)、队列列表(5)、面板窗口(11)、功能特性(8)、唱片架视图(2)
- 所有图标统一规范：24x24 viewBox, stroke-width:2, stroke="currentColor"
- 提供两种使用方式：直接导入单个组件 或 使用 Icon.vue 动态切换
- 创建了 barrel export (index.ts) 和统一 Icon.vue 动态组件
- 生成了 preview.html 图标库预览文档页面
- 项目原有的 TS 错误与新增图标组件无关，图标组件无编译错误

## 老项目 vs 新项目对比分析与 Emoji→SVG 替换
- 完成对 old-Mineradio（纯HTML/CSS/JS ~1.3MB单文件SPA）与新版 Vue+Electron 项目的全面对比分析
- **核心结论**：界面差异的最大根源是新版用 Emoji 替代了老版精心设计的 SVG 图标
- 新增 12 个缺失的 SVG 图标组件：IconHome, IconHeart, IconSun, IconMoon, IconCloudSun, IconCalendar, IconTrophy, IconPalette, IconMonitor, IconEye, IconEyeOff, IconUpload
- 替换 MainView.vue 工具栏所有 Emoji（11个按钮）→ SVG Icon 组件
- 替换 PlayerBar.vue 控制栏所有 Emoji（15+个按钮）→ SVG Icon 组件
- 修复 ctrl-btn/icon-btn 样式支持 SVG 图标（21px尺寸、stroke渲染、hover glow、fill+drop-shadow for liked状态）
- 修复 play-btn 恢复老项目58px毛玻璃圆形设计+SVG backdrop-filter
- 添加 html.control-glass-svg-ok 检测的 SVG 滤镜增强版
- 构建成功验证（vite build 5.93s）

## 全面替换为老项目原版 SVG 图标
- 用户要求所有图标采用老项目原有设计，老项目没有的用 emoji 暂代
- 替换了 15 个核心 SVG 图标组件为老项目原版路径：
  - **IconHome**: house+door path (stroke-width:1.9, 尺寸19)
  - **IconHeart**: fill-only heart (M12 21.45...), liked 时 #ff7a90 + drop-shadow
  - **IconPlay**: fill triangle (M8 5v14l11-7z), 尺寸20
  - **IconPause**: fill two rects, 尺寸20
  - **IconSkipBack**: fill bar+triangle (M6 6h2v12H6zm3.5 6l8.5 6V6z), 尺寸18
  - **IconSkipForward**: fill triangle+bar (M6 18l8.5-6L6 6v12zM16 6v12h2V6h-2z), 尺寸18
  - **IconRepeat**: stroke repeat arrows, 尺寸19
  - **IconRepeat1**: repeat arrows + "1" indicator (M12 9v6...), 尺寸19
  - **IconShuffle**: cross-shuffle paths, 尺寸19
  - **IconFolder**(collect): stroke plus (M12 5v14 M5 12h14), 尺寸18
  - **IconListOrdered**: stroke list with dots, 尺寸19
  - **IconVolume**: speaker + 2 wave arcs, 尺寸18
  - **IconVolumeX**: speaker + X cross, 尺寸18
  - **IconEye**(controls-hide): stroke 3 horizontal lines (decreasing width), 尺寸19
  - **IconMonitor**(immersive): focus corners + center circle, 尺寸19
  - **IconMaximize**: 4 corner brackets, 尺寸18
  - **IconPalette**(fx): 2 slider rows, 尺寸21
  - **IconUpload**: upload arrow, 尺寸18
  - **IconSearch**: magnifying glass, 尺寸17
- MainView 工具栏更新：
  - Home → 老项目 SVG IconHome
  - 视觉控制台 → 老项目 SVG IconPalette
  - 帮助 → 老项目 `?` 文字（与 #visual-guide-btn 一致）
  - 天气电台/每日推荐/排行榜/最近播放/歌词/主题切换/迷你模式/设置 → emoji 暂代（老项目没有这些独立按钮）
- PlayerBar 更新：
  - 歌词按钮 → 老项目 `<span class="lyrics-word-icon">词</span>` 纯文字
  - 控制条隐藏 → 老项目三层横线（不分 active/inactive 版本）
  - 移除 IconInfo, IconEyeOff, IconMusic import
- 构建成功验证（vite build 6.88s）

## P1/P2/P3 剩余优化项完成
- **P1 音质选择 pill 样式**：完全重构 QualitySelector.vue，恢复老项目 pill 药丸形按钮 + popover 弹层 + 5级音质 + SVIP锁定
- **P1 播放按钮 SVG 滤镜增强**：
  - 添加 `generateControlGlassDisplacementMap()` 动态位移贴图生成函数
  - 为三个 SVG 滤镜（control-glass/search-box/search-pill）注入 data URI 位移贴图
  - `checkSvgBackdropFilter()` 增加 Safari/Firefox 排除
  - `updateGlassChromaticOffset()` + fx store 集成，支持 VisualConsole 实时调整色差偏移
- **P2 唱片纹路修复**：
  - `.home-disc::before` 恢复老项目原版（inset:34% + linear-gradient + box-shadow）
  - `.disc-ring` 恢复老项目外圈细纹（asymmetric border colors）
  - `.card-cover-mini.placeholder::before` 添加 vinyl disc pattern（repeating-radial-gradient + conic-gradient）
  - 添加 6 组 tone 配色（card-weather/card-daily/card-fm/card-continue/card-profile/card-artists）
- **P3 用户胶囊样式对齐**：
  - hide-btn 恢复老项目 pill 形状（18×28px, opacity:0 → hover reveal）
  - user-capsule 基础样式：font-weight:650, letter-spacing:.08em, backdrop-filter
- **P3 搜索面板样式对齐**：
  - 添加来源区分 hover 色（netease红色调/qq绿色调）
  - 添加 .tag-source 样式（pill 形状 + 来源配色）
- 构建成功验证（vite build 7.66s）

## 系统性 UI/逻辑对比与同步
- 按照欢迎页→主页面容器→逐层深入的策略，系统性对比老项目和新项目所有功能模块
- **Phase 1 欢迎页**：完全重写 VisualGuide.vue，从简单居中弹窗→老项目交互式导览系统
  - 新增 visual-guide-scrim（gradient scrim + --gx/--gy CSS 变量）
  - 新增 visual-guide-ring（高亮聚光灯 + border + box-shadow + inner border + scan line）
  - 新增 visual-guide-card（kicker + title + body + hint + progress + actions）
  - 新增 getTargetRect() + positionStep() 动态定位逻辑
  - 6步内容完全匹配老项目原版文字
  - 新增 peek/reveal 事件处理
  - 新增 maybeAutoStartGuide() 自动启动逻辑
  - 新增 body.visual-guide-active CSS class 效果
- **Phase 2 主页面容器**：新项目已完整实现老项目的所有核心交互功能（7个关键功能全部等价或更好）
- **Phase 3 顶部工具栏**：老项目右上角极简（Home+登录+Fx FAB），新项目工具栏右侧更多按钮是新增功能
- **关键发现**：
  - 老项目 `#top-right` 只有3元素（Home + 登录 + hide-btn）
  - 新项目7个关键交互逻辑全部已实现（peek/DIY/bottom bar/用户胶囊/Fx FAB/沉浸/钉住）
  - 老项目缺失但新项目有的组件：#thumb-wrap(左下小封面)、#upload-actions+tip(搜索框旁上传)
- 构建成功验证（vite build 6.92s）

## 3个缺失组件实现
- **#thumb-wrap 左下迷你封面**：创建 `src/components/player/ThumbWrap.vue`
  - 老项目原版：fixed 定位 left:24px bottom:24px, 64px 圆角封面+标题+歌手信息
  - 可点击标题/歌手打开详情，hover 时 #fff0bf champagne 高亮
  - 封面随播放状态脉动（CSS breathing animation，老项目用 bass*0.08 缩放）
  - 沉浸模式自动隐藏
  - 已集成到 MainView.vue 模板
- **#upload-actions + #upload-tip 搜索框旁上传**：创建 `src/components/search/UploadActions.vue`
  - 老项目原版：搜索框旁 upload-btn（青色边框+icon-btn 样式）+ clear-cover-btn + tip 气泡
  - 上传按钮：老项目 SVG IconUpload + 青色(border-color rgba(0,245,212,.24)) hover 变亮
  - 上传提示气泡：老项目原版样式（gradient background + champagne border + arrow + title + close btn）
  - 首次显示逻辑：maybeShowUploadTipOnce，6.8秒自动关闭，localStorage 记住已看过
  - 清除封面按钮：28px mini circle，has-cover 状态时 champagne 风格
  - 已集成到 MainView.vue 的 app-content 区域
- **#idle-guide-canvas Canvas粒子动画**：完全重写 `src/components/guide/IdleGuide.vue`
  - 从老项目提取完整 Canvas 2D 粒子动画系统（240个粒子+3D透视+拖尾+锚点+引力交互）
  - 环形粒子76%（轨道运动+椭圆形+3D透视投影）+ 游离粒子24%（随机漂移）
  - 4个锚点 + 26帧拖尾系统（drawIdleGuideTrail）
  - 指针交互：pointerdown/move/up 拖拽旋转 + wheel 缩放 + pointer 引力吸引粒子
  - 惯性旋转衰减、呼吸光晕、粒子间连线
  - 通过 body.idle-guide-on / idle-guide-interactive / idle-guide-dragging 控制 CSS
  - 沉浸模式和播放中自动隐藏（opacity:0 transition 0.7s）
- 构建成功验证（vite build 8.11s）
